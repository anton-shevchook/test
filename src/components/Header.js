import React from 'react';

const Header = (props) => {
	const { video } = props;
	return (
		<header class="header">
			<div class="logo">UMulticultural</div>
		</header>
	);
};

export default Header;